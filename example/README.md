# moniter Example

## Get Started

1. Install Packages:

```bash
npm install
```

2. Build and Run the `moniter`! (Check `src/config/moniter` to see what is actually being monitored)

```bash
npm run start
```