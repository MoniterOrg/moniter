import IUrlConfig from "../interfaces/IUrlConfig";

const config: IUrlConfig = {
    "urls": [
        "https://google.com",
        "https://twitter.com",
        "https://facebook.com"
    ]
}

export default config;